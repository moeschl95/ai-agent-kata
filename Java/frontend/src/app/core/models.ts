export interface ShopItem {
  name: string;
  sellIn: number;
  quality: number;
  price: number;
}

export interface ProjectedItem {
  name: string;
  sellIn: number;
  quality: number;
  price: number;
}
